/**
 * sets the attribute with the title and the name in the string
 * example: element.setAttribute('class', 'button')
 * @param element
 * @param attributeType
 * @param attributeName
 */
export const setAttribute = (element, attributeType, attributeName) => {
  element.setAttribute(attributeType, attributeName);
};
